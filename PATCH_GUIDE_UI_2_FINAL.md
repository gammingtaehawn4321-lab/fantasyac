# 판타지악 UI 2.0 최종 마감 패치

## 이번 패치 범위
이 패치는 프론트엔드 UI만 수정합니다. `server.ts`, 성인 시스템 계산, body payload 계산, 저장/로드 데이터 구조, 전투 시스템은 변경하지 않았습니다.

## 핵심 변경
- 입력창 위 `[상태] [가방] [성장] [모험] [동료]` 5버튼을 모바일에서도 유지하면서 터치 영역과 간격을 보정했습니다.
- 모달 배경/최대 높이/전환 효과를 검정·금색 UI 2.0 기준으로 통일했습니다.
- 장비창은 기존 `AdaptiveCharacterImage`를 그대로 사용하며 캐릭터 삽화를 `contain` 방식으로 유지합니다.
- 내부상태의 사용자 이미지 역시 `object-contain`으로 변경하여 원본 비율과 전체 그림이 잘리지 않게 했습니다.
- 인벤토리는 iPad/PC에서 좌측 목록 + 우측 아이템 상세 프리뷰의 2열 레이아웃으로 확장했습니다.
- 모바일에서는 아이템을 누르면 기존 상세 모달이 즉시 열립니다.
- iPad/PC에서는 아이템 선택 후 우측에서 삽화·설명·품질·무게를 확인하고 `상세 정보 · 사용 / 장착`으로 기존 액션 모달을 엽니다.
- UI에 존재하던 일부 장식용 이모지 헤더를 제거했습니다.

## 그대로 보존한 것
- `server.ts`
- `adultNarrativeStyle.ts`
- `adultNarrativeDirectives.ts`
- `bodyLoadNarrativeDirectives.ts`
- `bodySystemUserRules.ts`
- 모든 성인 시스템 계산 및 감지
- body payload / 임신 / 기생체 / 방광 계산
- 인벤토리 실제 사용/장착/폐기 함수
- 전투 시스템
